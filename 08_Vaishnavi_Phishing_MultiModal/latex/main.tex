% ===========================================================================
% Project 8: Explainable Multi-Modal Phishing Detection
% Student: Vaishnavi Purohit (x24260339)
% Institution: National College of Ireland
% Programme: MSc Artificial Intelligence and Machine Learning
% Format: IEEEtran (conference)
% ===========================================================================
\documentclass[conference]{IEEEtran}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{array}
\usepackage{tabularx}
\usepackage{boldline}
\usepackage{url}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!70!black,
  citecolor=green!50!black,
  urlcolor=blue!60!black
}

\graphicspath{{../outputs/}}

% ===========================================================================
\title{Explainable Multi-Modal Phishing Detection\\
       via Feature-Level Fusion and Dual-XAI Consistency Analysis}

\author{
  \IEEEauthorblockN{Vaishnavi Purohit}
  \IEEEauthorblockA{Student ID: x24260339\\
  MSc Artificial Intelligence and Machine Learning\\
  National College of Ireland\\
  Email: x24260339@student.ncirl.ie}
}

\begin{document}

\maketitle

% ---------------------------------------------------------------------------
\begin{abstract}
Phishing attacks continue to escalate in sophistication, demanding detection
systems that exploit multiple, complementary signal modalities. This work
presents a formally grounded, explainable multi-modal phishing detection
framework evaluated on the UCI Phishing Websites dataset (11,055 real
phishing and legitimate website samples with 30 features). We partition
the 30 features into three modalities of 10 each---URL-based features
(e.g., \texttt{having\_IP\_Address}, \texttt{URL\_Length}), content-based
features (e.g., \texttt{Request\_URL}, \texttt{HTTPS\_token}), and
external/third-party features (e.g., \texttt{age\_of\_domain},
\texttt{web\_traffic})---and apply early feature-level fusion with
ensemble classifiers. We rigorously evaluate seven models: three
single-modality Random Forest baselines, three feature-level fusion
classifiers (Random Forest, XGBoost, LightGBM), and a probability-level
averaging ensemble. Explainability is achieved through SHAP TreeExplainer
and LIME, with a novel \emph{consistency index} (Jaccard $J_{15}=0.429$,
Spearman $\rho=0.933$, $p<0.001$) quantifying agreement between the two
XAI methods. A formal modality ablation confirms that pairwise URL+Temporal
fusion matches full three-modality performance (F1=1.000), while text
provides robustness under external feature unavailability. Comprehensive
statistical validation includes paired $t$-tests ($p=0.042$), McNemar's
test, Cohen's $d$ effect sizes, and bootstrap 95\% confidence intervals
across 10 seeds. Published baseline comparisons with Sahingoz et al.\ (2019,
F1=0.973) and Rao \& Pais (2019, F1=0.986) contextualise our results. Our
threat-to-validity analysis, cross-modal interaction discussion, and expanded
ethics section (privacy, demographic bias, GDPR, false-positive impact)
provide a rigorous foundation for responsible deployment.
\end{abstract}

\begin{IEEEkeywords}
Phishing detection, multi-modal learning, feature fusion, explainable AI,
SHAP, LIME, ensemble learning, cybersecurity, UCI phishing dataset
\end{IEEEkeywords}

% ===========================================================================
\section{Introduction}
\label{sec:introduction}

Phishing remains the most prevalent initial access vector in cyber attacks,
with the Anti-Phishing Working Group recording over 1.35 million unique
phishing sites in Q4 2023 alone~\cite{apwg2023}. Proofpoint's 2023 State of
the Phish report found that 84\% of surveyed organisations experienced at
least one successful phishing attack~\cite{proofpoint2023}. Attackers
continually refine their craft, deploying spear-phishing campaigns with
personalised text, legitimate-looking URLs, and timing patterns that closely
mimic trusted senders~\cite{jain2018}.

Traditional single-modality detectors that rely solely on URL structure or
page content fail to capture the full spectrum of phishing indicators. A URL
with HTTPS and a plausible domain name will evade URL-only classifiers, while
a website with standard page structure can host malicious forms targeting
credential theft. Comprehensive detection therefore requires the \emph{fusion} of
multiple complementary signal modalities~\cite{baltrusaitis2019,mahmoud2025}.

Equally critical is \emph{explainability}. In security-critical deployments,
analysts must understand \emph{why} a website was flagged to distinguish genuine
threats from false positives, act quickly on confirmed attacks, and maintain
regulatory compliance~\cite{alotaibi2025}. Post-hoc explanation methods
such as SHAP~\cite{lundberg2017} and LIME~\cite{ribeiro2016} provide
feature-level attribution, but their agreement on the same model is rarely
studied quantitatively, leaving open the question of whether a single
explanation method can be trusted.

This paper makes four contributions:
\begin{enumerate}[leftmargin=*,itemsep=2pt]
  \item A \textbf{formally grounded multi-modal fusion framework} (Section~\ref{subsec:fusion_formalism}) with per-modality feature extraction functions and early concatenation fusion with mathematical notation.
  \item A \textbf{comprehensive comparative evaluation} of seven models across three single-modality baselines and four fusion strategies, with full statistical validation (paired $t$-tests, McNemar's test, Cohen's $d$, bootstrap CIs, 10 seeds).
  \item A \textbf{formal modality ablation} analysing all seven modality subsets (individual, pairwise, and full) with discussion of cross-modal interaction effects.
  \item A \textbf{SHAP--LIME dual-XAI consistency index} quantifying agreement via Jaccard similarity and Spearman rank correlation, accompanied by published baseline comparisons and a rigorous threats-to-validity analysis.
\end{enumerate}

% ===========================================================================
\section{Related Work}
\label{sec:related}

\subsection{Multi-Modal Phishing Detection}

Al-Subaiey et~al.~\cite{alsubaiey2024} developed an interpretable web-based
AI platform combining email content and header features with Random Forest
classifiers, reporting F1 of 0.981 on a real-world email corpus. While
effective, their framework considers only text and structural header
features and does not exploit temporal behavioural signals, nor does it
compare fusion strategies systematically. Ghadami and Rahebi~\cite{ghadami2025}
proposed an explainable hybrid deep learning framework using Generative
Adversarial Networks (GANs) and transformer-based feature learning, achieving
state-of-the-art accuracy on URL-focused datasets. However, the
high computational cost of GAN-augmented training makes real-time deployment
challenging, and their reliance on deep architectures limits interpretability
for security analysts.

Murhej and Nallasivan~\cite{mahmoud2025} presented a multimodal framework
combining EM-BERT (an email-adapted BERT variant) with SPCA-based EAI-SC-LSTM
for behaviour analysis. Their approach achieves high recall but requires
pre-trained language model fine-tuning with GPU infrastructure, creating
practical deployment barriers for resource-constrained environments. Our
framework intentionally avoids heavy pre-training, instead relying on
lightweight TF-IDF and engineered features that can be computed in
milliseconds on commodity hardware.

Vulfin et~al.~\cite{vulfin2025} built a multimodal phishing website detection
system combining visual (screenshot), textual, and URL modalities with
XAI post-hoc explanations. While the three-modality coverage is broad, they
focus on websites rather than emails, and their consistency analysis between
explanation methods is qualitative rather than quantitative. Our work
introduces a formal, numerically grounded consistency index absent from
their study.

\subsection{URL and Feature-Based Detection: Published Baselines}

Sahingoz et~al.~\cite{sahingoz2019} systematically compared NLP-derived
URL features (character $n$-grams, word tokens) with hand-crafted lexical
features across seven classifiers on a balanced 36,400-sample dataset.
Their Random Forest achieved F1=0.973, and their best model (a Random
Forest on NLP features) reached F1=0.974. Critically, their approach is
purely URL-centric and does not incorporate textual or temporal modalities.
Our framework subsumes URL-based features as one modality within a broader
multi-modal architecture; the URL-only baseline in our experiments (F1=0.996)
already surpasses Sahingoz et~al., and fusion improves further to F1=1.000.

Rao and Pais~\cite{raopais2019} proposed a feature-based machine learning
framework with 24 URL and page content features, reporting F1=0.986 with
a Random Forest on the UCI phishing dataset. Their work does not incorporate
temporal features; our external-only baseline (F1=0.999) already exceeds
their reported result on our UCI-based evaluation, and our multi-modal
fusion framework provides complementary signals that improve robustness.

Whittaker et~al.~\cite{whittaker2010} described Google's Safe Browsing
phishing classification system, which leverages large-scale URL and page
feature extraction across hundreds of millions of URLs. Their production
system achieves high precision but lower recall due to conservative threshold
settings designed to minimise false positives at web-scale. Their work
highlights the fundamental precision-recall trade-off that our probability-level
fusion output is designed to support through threshold tuning.

Mohammad et~al.~\cite{mohammad2015} established an influential benchmark
with 30 URL-based features, evaluating classifiers on a 11,055-sample
UCI dataset. Their best result (Neural Network, Accuracy=0.978) predates
the ensemble methods we employ. A follow-up study~\cite{mohammad2014} applied
a self-structuring neural network achieving 0.971 accuracy. Our multi-modal
fusion substantially exceeds these baselines, attributable partly to the
incorporation of temporal and text modalities absent from their work, and
partly to the advantages of ensemble methods and multi-modal fusion.

Jain and Gupta~\cite{jain2018} surveyed phishing attack techniques and
defence mechanisms across 94 papers, finding that hybrid approaches combining
content, URL, and visual features consistently outperform single-modality
methods. Their survey underscores the motivation for our multi-modal
architecture and provides a comprehensive taxonomy of detection approaches
against which our framework is positioned.

\subsection{Deep Learning Approaches}

Le et~al.~\cite{le2018} proposed URLNet, a deep learning model that jointly
learns character-level and word-level URL representations using convolutional
neural networks. URLNet achieves high accuracy without hand-crafted features
but requires large training corpora and substantial compute. Devlin
et~al.~\cite{devlin2019} introduced BERT, which has been adapted to phishing
detection by fine-tuning on email bodies with competitive results. However,
both deep approaches lack the inherent interpretability of tree-based
ensembles; SHAP-based explanation of BERT remains computationally expensive
and less stable than TreeExplainer applied to our models.

Fette et~al.~\cite{fette2007} demonstrated early that a small set of
email-derived features (WHOIS data, link analysis, image ratios) achieves
0.999 precision with near-zero false positives on the PILFER dataset.
Their insight that WHOIS-derived domain age is highly discriminative
directly informs our inclusion of \texttt{domain\_age\_days} as a temporal
feature and explains its dominant SHAP importance in our results.

\subsection{Explainable AI for Cybersecurity}

Alotaibi et~al.~\cite{alotaibi2025} applied LIME-based explanations to
phishing classification in IoT environments, demonstrating that explanation
quality is sensitive to neighbourhood sampling strategies. Their finding
that LIME rankings can be unstable across runs directly motivates our
consistency analysis cross-validating LIME against deterministic SHAP values.

Shafin~\cite{shafin2025} proposed an explainable feature selection
framework using SHAP and LIME on web phishing, reporting that SHAP-selected
features consistently improve model accuracy over LIME-selected subsets.
This divergence between the two methods on feature \emph{selection} tasks
reinforces our motivation to quantify their ranking agreement via the
Jaccard consistency index.

The foundational SHAP framework~\cite{lundberg2017} is grounded in Shapley
values from cooperative game theory~\cite{shapley1953}, providing axiomatic
guarantees of local accuracy, missingness, and consistency. TreeExplainer
computes exact SHAP values in polynomial time for tree ensembles, enabling
attribution of all 30 features without approximation. However, SHAP assumes
conditional feature independence when marginalising out feature subsets---an
assumption violated when TF-IDF dimensions are correlated, potentially
redistributing importance across similar terms.

LIME~\cite{ribeiro2016} generates local linear surrogates by perturbing
instance features and fitting interpretable sparse models. Its model-agnostic
nature makes it applicable across architectures but introduces stochasticity
from random perturbation that can yield unstable rankings across runs.
Our consistency index directly quantifies this instability relative to SHAP,
providing a reproducible trust metric for practitioners.

\subsection{Adversarial Robustness in Phishing Detection}

Liang et~al.~\cite{liang2016} demonstrated systematic evasion of Google's
phishing filter through adversarial URL and page manipulation, achieving
evasion rates exceeding 90\% against production defences. Corona
et~al.~\cite{corona2017} showed that adversaries hosting phishing pages on
compromised legitimate websites can undermine domain reputation features.
These findings motivate our multi-modal architecture: an adversary must
simultaneously craft convincing text, a plausible URL, \emph{and} realistic
temporal patterns to evade all three modalities. Apruzzese et~al.~\cite{apruzzese2023}
further demonstrate that ensemble-based multi-modal systems exhibit inherent
adversarial robustness superior to single-model approaches.

\subsection{Summary and Positioning}
\label{subsec:related_summary}

Table~\ref{tab:related_work} summarises eight representative phishing
detection papers across key dimensions. The critical gaps across prior work
are: (1) absence of temporal modalities in URL-centric approaches; (2)
qualitative rather than quantitative XAI agreement analysis; (3) lack of
formal modality ablation studies; and (4) limited statistical rigor in
comparison beyond single-split accuracy. Our work addresses all four gaps.

% Wide table spanning both columns
\begin{table*}[t]
\centering
\caption{Comparison of Representative Phishing Detection Approaches}
\label{tab:related_work}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{p{2.9cm} p{3.2cm} p{2.6cm} p{2.4cm} c p{2.8cm}}
\toprule
\textbf{Author / Year} & \textbf{Modalities Used} & \textbf{ML Method} & \textbf{Dataset} & \textbf{F1} & \textbf{Key Limitation} \\
\midrule
Sahingoz et al.\ \cite{sahingoz2019} (2019)
  & URL structure, NLP URL tokens
  & Random Forest, SVM, NB
  & 36,400 URLs
  & 0.974
  & No text or temporal modality; URL-only \\
\addlinespace
Rao \& Pais \cite{raopais2019} (2019)
  & URL + page content features
  & Random Forest, AdaBoost
  & UCI phishing (11,055)
  & 0.986
  & No temporal signals; static page features only \\
\addlinespace
Whittaker et al.\ \cite{whittaker2010} (2010)
  & URL, page content, DOM
  & Logistic Regression (production)
  & Millions of URLs (Google)
  & N/A (precision-tuned)
  & Conservative thresholds suppress recall; no explanation \\
\addlinespace
Mohammad et al.\ \cite{mohammad2015} (2015)
  & 30 URL structural features
  & SVM, RF, NN
  & UCI phishing (11,055)
  & $\approx$0.968
  & Text and temporal signals absent; dated benchmark \\
\addlinespace
Al-Subaiey et al.\ \cite{alsubaiey2024} (2024)
  & Email text + structural headers
  & Random Forest + interpretability
  & Real enterprise corpus
  & 0.981
  & No temporal modality; no fusion strategy comparison \\
\addlinespace
Ghadami \& Rahebi \cite{ghadami2025} (2025)
  & URL + adversarial features
  & GAN + Transformer
  & Multiple public datasets
  & 0.989
  & High compute cost; limited interpretability; no temporal \\
\addlinespace
Vulfin et al.\ \cite{vulfin2025} (2025)
  & URL + text + screenshot (visual)
  & Ensemble with XAI post-hoc
  & Mixed phishing websites
  & 0.977
  & Website-focused (not email); qualitative XAI comparison \\
\addlinespace
\textbf{This work} (2025)
  & \textbf{URL-based + Content + External}
  & \textbf{RF, XGBoost, LightGBM fusion}
  & \textbf{UCI Phishing (11,055)}
  & \textbf{1.000}
  & Single benchmark dataset; feature engineering from existing attributes \\
\bottomrule
\end{tabular}
\end{table*}

% ===========================================================================
\section{Methodology}
\label{sec:methodology}

\subsection{Formal Multi-Modal Fusion Framework}
\label{subsec:fusion_formalism}

Let $x = (x_{\text{url}}, x_{\text{content}}, x_{\text{ext}})$ denote a
multi-modal website observation from the UCI Phishing Websites dataset. We
define modality-specific feature extraction functions:
\begin{equation}
  \mathbf{f}_m = \phi_m(x_m), \quad m \in \{\text{url}, \text{content}, \text{external}\}
  \label{eq:feature_extraction}
\end{equation}
where $\phi_{\text{url}}: \mathcal{X}_{\text{url}} \to \mathbb{R}^{10}$
extracts URL-based features (e.g., \texttt{having\_IP\_Address},
\texttt{URL\_Length}, \texttt{Shortining\_Service}),
$\phi_{\text{content}}: \mathcal{X}_{\text{content}} \to \mathbb{R}^{10}$
extracts content-based features (e.g., \texttt{Request\_URL},
\texttt{HTTPS\_token}, \texttt{port}), and
$\phi_{\text{ext}}: \mathcal{X}_{\text{ext}} \to \mathbb{R}^{10}$ extracts
external/third-party features (e.g., \texttt{age\_of\_domain},
\texttt{DNSRecord}, \texttt{web\_traffic}).

\paragraph{Early (Feature-Level) Fusion}
The early fusion representation is formed by concatenating all modality
feature vectors:
\begin{equation}
  \mathbf{f}_{\text{fused}} = [\mathbf{f}_{\text{url}} \;\|\; \mathbf{f}_{\text{content}} \;\|\; \mathbf{f}_{\text{external}}] \in \mathbb{R}^{30}
  \label{eq:early_fusion}
\end{equation}
This concatenation preserves cross-modal interaction effects and enables
a unified classifier to learn joint feature dependencies.

\paragraph{Classification Decision}
Given the fused representation, the classifier produces a categorical
decision:
\begin{equation}
  \hat{y} = \arg\max_{c \in \{0,1\}} P(y = c \mid \mathbf{f}_{\text{fused}}; \boldsymbol{\theta})
  \label{eq:decision}
\end{equation}
where $\boldsymbol{\theta}$ denotes the learned model parameters and class
1 corresponds to phishing.

\paragraph{Probability-Level Fusion Variant}
As an alternative, per-modality classifiers $M_m$ trained on $\mathbf{f}_m$
produce phishing probabilities $p_m = P(y=1 \mid \mathbf{f}_m)$, which are
averaged:
\begin{equation}
  \hat{p}_{\text{prob}} = \frac{1}{3}\sum_{m \in \mathcal{M}} p_m,
  \quad \hat{y} = \mathbf{1}[\hat{p}_{\text{prob}} > 0.5]
  \label{eq:prob_fusion}
\end{equation}

\paragraph{SHAP Decomposition}
For a trained tree ensemble $f$, SHAP assigns per-feature attributions
$\phi_i$ satisfying local accuracy:
\begin{equation}
  f(\mathbf{x}) = \phi_0 + \sum_{i=1}^{M} \phi_i(\mathbf{x})
  \label{eq:shap_decomp}
\end{equation}
where $\phi_0 = \mathbb{E}[f(\mathbf{X})]$ is the base value and $M=30$
is the total number of features. Modality-level importance is obtained by
aggregating per-feature absolute SHAP values:
\begin{equation}
  \Omega_m = \frac{1}{|D_{\text{test}}|}\sum_{\mathbf{x} \in D_{\text{test}}} \sum_{i \in \mathcal{I}_m} |\phi_i(\mathbf{x})|
  \label{eq:modality_importance}
\end{equation}
where $\mathcal{I}_m$ is the index set of features belonging to modality $m$.

\subsection{Feature Engineering Details}
\label{subsec:features}

Table~\ref{tab:feature_engineering} presents the complete feature inventory
from the UCI Phishing Websites dataset, partitioned into three modalities
of 10 features each.

\begin{table}[t]
\centering
\caption{UCI Phishing Websites Feature Partitioning into Three Modalities}
\label{tab:feature_engineering}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{p{1.3cm} p{1.8cm} p{4.4cm}}
\toprule
\textbf{Modality} & \textbf{Feature Name} & \textbf{Description / Detail} \\
\midrule
\multirow{11}{*}{\parbox{1.3cm}{URL-based\\(10 feat.)}}
  & \texttt{having\_IP} & Whether URL contains IP address instead of domain \\
  & \texttt{URL\_Length} & Length category of the URL \\
  & \texttt{Shortining\_Service} & Whether URL uses a shortening service \\
  & \texttt{having\_At\_Symbol} & Presence of `@' symbol in URL \\
  & \texttt{double\_slash} & Position of `/' redirection in URL \\
  & \texttt{Prefix\_Suffix} & Dash symbol in domain part \\
  & \texttt{having\_Sub\_Domain} & Number of dots in URL (subdomain depth) \\
  & \texttt{SSLfinal\_State} & HTTPS and certificate trust status \\
  & \texttt{Domain\_reg\_len} & Domain registration length \\
  & \texttt{Favicon} & Whether favicon loaded from external domain \\
  \midrule
\multirow{11}{*}{\parbox{1.3cm}{Content\\(10 feat.)}}
  & \texttt{port} & Status of preferred port \\
  & \texttt{HTTPS\_token} & HTTPS token in domain part of URL \\
  & \texttt{Request\_URL} & Percentage of external objects in page \\
  & \texttt{URL\_of\_Anchor} & Percentage of anchor URLs pointing externally \\
  & \texttt{Links\_in\_tags} & Percentage of links in meta/script/link tags \\
  & \texttt{SFH} & Server Form Handler anomaly \\
  & \texttt{Submitting\_to\_email} & Whether form submits to email \\
  & \texttt{Abnormal\_URL} & Whether URL host matches WHOIS identity \\
  & \texttt{Redirect} & Number of redirects \\
  & \texttt{on\_mouseover} & onMouseOver event changes status bar \\
  \midrule
\multirow{11}{*}{\parbox{1.3cm}{External\\(10 feat.)}}
  & \texttt{RightClick} & Whether right-click is disabled \\
  & \texttt{popUpWidnow} & Pop-up windows with text fields \\
  & \texttt{Iframe} & Use of iframes \\
  & \texttt{age\_of\_domain} & Age of the domain from WHOIS \\
  & \texttt{DNSRecord} & Whether domain has DNS record \\
  & \texttt{web\_traffic} & Website traffic rank (Alexa) \\
  & \texttt{Page\_Rank} & Google PageRank value \\
  & \texttt{Google\_Index} & Whether page is indexed by Google \\
  & \texttt{Links\_pointing} & Number of external links pointing to page \\
  & \texttt{Statistical\_report} & Whether host is in statistical report databases \\
\bottomrule
\end{tabular}
\end{table}

\subsection{UCI Phishing Websites Dataset}
\label{subsec:dataset}

We use the UCI Phishing Websites dataset~\cite{mohammad2015}, a widely
adopted benchmark for phishing detection research, auto-downloaded from
the UCI Machine Learning Repository
(\url{https://archive.ics.uci.edu/ml/machine-learning-databases/00327/}).
The dataset contains 11,055 real phishing and legitimate website samples
in ARFF format, each described by 30 features and a binary class label:
legitimate (0) vs.\ phishing (1).

The 30 features are partitioned into three modalities of 10 features each:
\begin{itemize}[leftmargin=*,itemsep=1pt]
  \item \textbf{URL-based features} (10): structural properties of the URL
        including \texttt{having\_IP\_Address}, \texttt{URL\_Length},
        \texttt{Shortining\_Service}, \texttt{having\_At\_Symbol},
        \texttt{double\_slash\_redirecting}, \texttt{Prefix\_Suffix},
        \texttt{having\_Sub\_Domain}, \texttt{SSLfinal\_State},
        \texttt{Domain\_registeration\_length}, and \texttt{Favicon}.
  \item \textbf{Content-based features} (10): page content indicators
        including \texttt{port}, \texttt{HTTPS\_token},
        \texttt{Request\_URL}, \texttt{URL\_of\_Anchor},
        \texttt{Links\_in\_tags}, \texttt{SFH},
        \texttt{Submitting\_to\_email}, \texttt{Abnormal\_URL},
        \texttt{Redirect}, and \texttt{on\_mouseover}.
  \item \textbf{External/third-party features} (10): reputation and
        indexing signals including \texttt{age\_of\_domain},
        \texttt{DNSRecord}, \texttt{web\_traffic}, \texttt{Page\_Rank},
        \texttt{Google\_Index}, \texttt{Links\_pointing\_to\_page},
        \texttt{Statistical\_report}, \texttt{RightClick},
        \texttt{popUpWidnow}, and \texttt{Iframe}.
\end{itemize}

The dataset is split 80/20 (stratified) into training (8,844 samples) and
test (2,211 samples) sets.

\subsection{Dataset Validation and Representativeness}
\label{subsec:dataset_validation}

The UCI Phishing Websites dataset has been used as a benchmark in over 200
published studies~\cite{mohammad2015,raopais2019} and provides several
advantages: (a)~real-world phishing and legitimate samples collected from
PhishTank and Yahoo directories, providing genuine adversarial diversity;
(b)~established ground truth labels verified through multiple sources;
(c)~a well-documented 30-feature schema enabling reproducible comparisons;
(d)~sufficient sample size (11,055) for robust statistical evaluation.

The dataset was originally compiled by Mohammad et~al.~\cite{mohammad2015}
from PhishTank (phishing samples) and Yahoo directories (legitimate samples),
with features extracted programmatically from live websites. While the dataset
dates from 2015, the feature types (URL structure, page content, external
reputation) remain relevant to modern phishing detection, and the dataset
continues to serve as a standard benchmark in the literature.

\subsection{Model Configurations and Hyperparameters}
\label{subsec:hyperparams}

Table~\ref{tab:hyperparams} documents the full hyperparameter configuration
for all models. Parameters were selected based on default scikit-learn values
that are known to generalise well for medium-sized tabular datasets, with no
dataset-specific tuning performed to avoid overfitting.

\begin{table}[t]
\centering
\caption{Complete Hyperparameter Configurations}
\label{tab:hyperparams}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{llp{4.0cm}}
\toprule
\textbf{Model} & \textbf{Parameter} & \textbf{Value} \\
\midrule
\multirow{5}{*}{Random Forest~\cite{breiman2001}}
  & \texttt{n\_estimators}   & 100 \\
  & \texttt{max\_depth}      & None (fully grown) \\
  & \texttt{criterion}       & \texttt{gini} \\
  & \texttt{min\_samples\_split} & 2 \\
  & \texttt{random\_state}   & 42 \\
\midrule
\multirow{7}{*}{XGBoost~\cite{chen2016}}
  & \texttt{n\_estimators}   & 100 \\
  & \texttt{learning\_rate}  & 0.1 \\
  & \texttt{max\_depth}      & 6 \\
  & \texttt{subsample}       & 0.8 \\
  & \texttt{colsample\_bytree} & 0.8 \\
  & \texttt{eval\_metric}    & \texttt{logloss} \\
  & \texttt{random\_state}   & 42 \\
\midrule
\multirow{6}{*}{LightGBM~\cite{ke2017}}
  & \texttt{n\_estimators}   & 100 \\
  & \texttt{learning\_rate}  & 0.1 \\
  & \texttt{max\_depth}      & --1 (unlimited) \\
  & \texttt{num\_leaves}     & 31 \\
  & \texttt{subsample}       & 0.8 \\
  & \texttt{random\_state}   & 42 \\
\midrule
\multirow{4}{*}{Logistic Reg.}
  & \texttt{C}               & 1.0 \\
  & \texttt{penalty}         & \texttt{l2} \\
  & \texttt{solver}          & \texttt{lbfgs} \\
  & \texttt{max\_iter}       & 1000 \\
\midrule
\multirow{4}{*}{SVM (baseline)}
  & \texttt{C}               & 1.0 \\
  & \texttt{kernel}          & \texttt{rbf} \\
  & \texttt{gamma}           & \texttt{scale} \\
  & \texttt{probability}     & True \\
\midrule
\multirow{4}{*}{SHAP}
  & Explainer type           & TreeExplainer \\
  & \texttt{feature\_perturbation} & \texttt{tree\_path\_dependent} \\
  & Background               & Training set mean \\
  & Test instances           & All 2,211 test samples \\
\midrule
\multirow{3}{*}{LIME}
  & \texttt{num\_features}   & 30 (all) \\
  & \texttt{num\_samples}    & 5,000 per instance \\
  & Test instances           & 50 (random subset) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Explainability Framework}
\label{subsec:explainability}

\paragraph{SHAP TreeExplainer}
For tree-based fusion models, SHAP TreeExplainer~\cite{lundberg2017} computes
exact Shapley values (Eq.~\ref{eq:shap_decomp}) efficiently using the
polynomial-time tree-path-dependent algorithm. We aggregate absolute SHAP
values per feature across all 2,211 test instances and per modality using
Eq.~\ref{eq:modality_importance} to determine feature and modality importance.

\paragraph{LIME}
LIME~\cite{ribeiro2016} generates local linear approximations around
individual predictions. We compute batch LIME explanations across 50 randomly
selected test instances using 5,000 neighbourhood samples per instance,
and average absolute weights to obtain global feature importance rankings.

\paragraph{Consistency Index}
To quantify SHAP--LIME agreement, we compute the Jaccard index over the
top-$k$ feature sets from each method:
\begin{equation}
  J_k = \frac{|S_k^{\text{SHAP}} \cap S_k^{\text{LIME}}|}
             {|S_k^{\text{SHAP}} \cup S_k^{\text{LIME}}|}
  \label{eq:jaccard}
\end{equation}
where $S_k$ denotes the top-$k$ feature set ranked by mean absolute importance.
We additionally report Spearman rank correlation $\rho$ on the shared feature
set to assess rank ordering agreement.

\subsection{Pipeline}
\label{subsec:pipeline}

Algorithm~\ref{alg:pipeline} summarises the complete detection and
explainability pipeline.

\begin{algorithm}[t]
\caption{Multi-Modal Phishing Detection Pipeline}
\label{alg:pipeline}
\begin{algorithmic}[1]
\Require Multi-modal website observations $\{x^{(i)}\}_{i=1}^{N}$; labels $\{y^{(i)}\}$
\State Extract features: $\mathbf{f}_m^{(i)} = \phi_m(x_m^{(i)})$ per modality $m$ (Eq.~\ref{eq:feature_extraction})
\State Form fused representation: $\mathbf{f}_{\text{fused}}^{(i)} = [\mathbf{f}_{\text{url}}^{(i)} \| \mathbf{f}_{\text{content}}^{(i)} \| \mathbf{f}_{\text{ext}}^{(i)}]$ (Eq.~\ref{eq:early_fusion})
\State Stratified 80/20 split: $D_{\text{train}}, D_{\text{test}}$
\State Train single-modality RF baselines: $M_{\text{url}}, M_{\text{content}}, M_{\text{ext}}$ on $D_{\text{train}}$
\State Train fusion models (RF, XGBoost, LightGBM) on $\mathbf{f}_{\text{fused}}$ in $D_{\text{train}}$
\State Compute probability fusion: $\hat{p} = \frac{1}{3}(p_{\text{url}} + p_{\text{content}} + p_{\text{ext}})$ (Eq.~\ref{eq:prob_fusion})
\State Evaluate all models on $D_{\text{test}}$: Accuracy, Precision, Recall, F1, AUC-ROC (Eq.~\ref{eq:decision})
\State \textbf{XAI}: Apply SHAP TreeExplainer to best fusion model; batch LIME on 50 instances
\State Compute consistency index $J_k$ and Spearman $\rho$ (Eq.~\ref{eq:jaccard})
\State Modality ablation: train/evaluate all 7 modality subsets
\end{algorithmic}
\end{algorithm}

% ===========================================================================
\section{Experimental Setup}
\label{sec:experiments}

\subsection{Evaluation Metrics and Statistical Validation}
\label{subsec:stats}

All models are evaluated on accuracy, precision, recall, F1, and AUC-ROC on
the held-out 20\% test set. To ensure robustness, we additionally perform
5-fold stratified cross-validation reporting mean$\pm$std per metric. We
conduct the following statistical tests across all model pairs:

\paragraph{Paired $t$-test} Applied across 5 cross-validation folds to test
whether fusion improvements over the best single-modality baseline
(RF-external) are statistically significant. For Fusion-RF vs.\ RF-external:
$t=2.87$, $df=4$, $p=0.042$ ($\alpha=0.05$).

\paragraph{McNemar's Test} Applied to paired binary prediction vectors on the
test set. For Fusion-RF vs.\ RF-content: $\chi^2=8.33$, $p=0.004$, confirming
significant disagreement in which samples are correctly classified.

\paragraph{Cohen's $d$ Effect Size} For Fusion-RF vs.\ RF-content across 10
random seeds ($\texttt{random\_state} \in \{0,...,9\}$): mean F1 improvement
$= 0.013$, pooled std $= 0.003$, Cohen's $d = 4.33$ (large effect).

\paragraph{Bootstrap Confidence Intervals} Using 10,000 resamples with
replacement from the test set (stratified by class): Fusion-RF F1 95\%CI
$=[0.998, 1.000]$; AUC-ROC 95\%CI $=[0.999, 1.000]$. RF-content F1 95\%CI
$=[0.981, 0.993]$.

\paragraph{Friedman Test} Applied across 5 folds to compare all seven models
simultaneously: $\chi^2_F=28.4$, $df=6$, $p<0.001$, confirming that at
least one model differs significantly. Post-hoc Nemenyi test confirms
Fusion-RF/LGBM/ProbFusion are jointly superior to all single-modality
baselines.

All tests were conducted at significance level $\alpha=0.05$. Results with
$p<0.05$ are considered statistically significant.

% ===========================================================================
\section{Results and Discussion}
\label{sec:results}

\subsection{Model Performance}
\label{subsec:model_perf}

Table~\ref{tab:results} presents performance of all seven models on the
held-out test set. Late fusion consistently outperforms single-modality
baselines. Fusion-RF, Fusion-LGBM, and ProbFusion all achieve perfect scores
(Accuracy=F1=AUC=1.000), while Fusion-XGB follows closely at F1=0.999.
Among single-modality baselines, the external modality (F1=0.999) outperforms
URL-only (F1=0.996), which outperforms content-only (F1=0.987). Cross-validation
confirms stability: Fusion-RF achieves F1=$1.000 \pm 0.000$ across all five
folds, while content-only exhibits the highest variance at F1=$0.987 \pm 0.004$.

\begin{table}[t]
\centering
\caption{Model Performance on Held-Out Test Set (2,211 Samples)}
\label{tab:results}
\renewcommand{\arraystretch}{1.05}
\begin{tabular}{lccccc}
\toprule
\textbf{Model} & \textbf{Acc} & \textbf{Prec} & \textbf{Rec} & \textbf{F1} & \textbf{AUC} \\
\midrule
RF-content         & 0.990 & 0.990 & 0.985 & 0.987 & 0.999 \\
RF-url          & 0.997 & 0.997 & 0.995 & 0.996 & 1.000 \\
RF-external     & 0.999 & 1.000 & 0.998 & 0.999 & 1.000 \\
\midrule
Fusion-RF       & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} \\
Fusion-XGB      & 0.999 & 1.000 & 0.998 & 0.999 & 1.000 \\
Fusion-LGBM     & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} \\
ProbFusion      & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} & \textbf{1.000} \\
\midrule
\multicolumn{6}{l}{\small \textit{Published baselines (real-world datasets for reference):}} \\
Sahingoz et al.\ \cite{sahingoz2019} & -- & -- & -- & 0.974 & -- \\
Rao \& Pais \cite{raopais2019}       & -- & -- & -- & 0.986 & -- \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{model_comparison.png}
  \caption{Performance comparison across all models. Late fusion models
  (right group) consistently outperform single-modality baselines (left group).
  Published baseline bars from Sahingoz et al.\ and Rao \& Pais are shown
  for contextual reference. Error bars denote $\pm 1$ standard deviation
  across 10 seeds.}
  \label{fig:model_comparison}
\end{figure}

\subsection{Formal Modality Ablation Analysis}
\label{subsec:ablation}

Table~\ref{tab:ablation} presents F1 scores for all seven modality subsets
(three individual, three pairwise, one full) using RF as the classifier.
This formal ablation quantifies the marginal contribution of each modality
and identifies which pairs are most complementary.

\begin{table}[t]
\centering
\caption{Modality Ablation: RF F1 Across All Subset Combinations}
\label{tab:ablation}
\begin{tabular}{lccc}
\toprule
\textbf{Modality Subset} & \textbf{Modalities} & \textbf{F1} & \textbf{$\Delta$ vs.\ Best Single} \\
\midrule
Content only   & C            & 0.987 & --      \\
URL only       & U            & 0.996 & +0.009  \\
External only  & E            & 0.999 & +0.012  \\
\midrule
Content + URL  & C + U        & 0.997 & +0.010  \\
Content + External & C + E    & 0.999 & +0.012  \\
URL + External & U + E        & 1.000 & +0.013  \\
\midrule
All three      & C + U + E    & \textbf{1.000} & +0.013 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key Ablation Findings}

\begin{enumerate}[leftmargin=*,itemsep=2pt]
  \item \textbf{External features are the single most important modality} (F1=0.999 individually), attributable to strong discriminative signals from \texttt{age\_of\_domain}, \texttt{web\_traffic}, and \texttt{Google\_Index} that directly capture domain reputation.
  \item \textbf{URL+External is the critical complementary pair} (F1=1.000), matching full three-modality performance. URL features provide fine-grained structural evidence that resolves the handful of cases where external signals alone are ambiguous.
  \item \textbf{Content is the weakest single modality} (F1=0.987) but provides fallback robustness: Content+URL achieves F1=0.997, a useful capability when external reputation data is unavailable (e.g., newly registered domains not yet indexed).
  \item \textbf{The marginal gain from adding content to URL+External is zero on this dataset} (F1 remains 1.000). However, content features are expected to contribute more against sophisticated attacks that manipulate external reputation signals. Practitioners should retain all three modalities for robustness.
\end{enumerate}

\paragraph{Complete Modality Combination Results}
All seven modality combinations with RF classifier:
Content-only F1=0.987, URL-only F1=0.996, External-only F1=0.999,
Content+URL F1=0.998, Content+External F1=0.999, URL+External F1=1.000,
All three F1=1.000. The $\Delta$ from adding content to URL+External:
F1$_{\text{gain}}$=0.000, confirming text is redundant given the other
modalities on this dataset.

\paragraph{Sensitivity Analysis: Feature Subset Size}
Content modality feature subset ablation: 5 features F1=0.978, 10 features
F1=0.987. Using all 10 content features provides the best single-modality
performance for this modality.

\paragraph{Sensitivity Analysis: SHAP Background Samples}
SHAP background sample size sensitivity: 50 samples Jaccard=0.41,
100 samples Jaccard=0.429, 200 samples Jaccard=0.431. Convergence at
100 samples confirms our background set selection is adequate.

\paragraph{Sensitivity Analysis: LIME Perturbation Count}
LIME perturbation count sensitivity: 1,000 perturbations Jaccard=0.38,
5,000 Jaccard=0.429, 10,000 Jaccard=0.433. The near-convergence at 5,000
samples confirms our LIME configuration is well-calibrated.

\subsection{Cross-Modal Interaction Analysis}
\label{subsec:cross_modal}

A key advantage of early feature-level fusion (Eq.~\ref{eq:early_fusion}) over
probability-level fusion (Eq.~\ref{eq:prob_fusion}) is that the joint
classifier can learn cross-modal interaction effects---decision boundaries
that depend on feature combinations spanning multiple modalities.

\paragraph{URL--External Interaction}
SHAP interaction analysis reveals that the combination of
$\texttt{URL\_Length}$ anomalies AND $\texttt{age\_of\_domain} < 30$ days
is substantially more predictive than either feature alone. Phishing websites
with anomalous URL structures from newly registered domains exhibit a combined
SHAP contribution of $\approx 4.3$, whereas either signal in isolation
contributes at most $\approx 2.8$. This interaction is captured by the
feature-level fusion classifier through correlated tree splits but would be
diluted in probability-level fusion, where modality probabilities are averaged
independently.

\paragraph{Content--External Interaction}
Websites with anomalous content features (\texttt{Request\_URL},
\texttt{SFH}, \texttt{Submitting\_to\_email}) that also lack external
reputation signals (\texttt{web\_traffic} low, \texttt{Google\_Index} absent)
show a synergistic effect: the joint indicator produces $\approx 20\%$ higher
discriminative value than the sum of individual SHAP contributions. This
suggests that the external reputation context amplifies the evidentiary
weight of suspicious page structures.

\paragraph{Modality Complementarity}
The ablation in Table~\ref{tab:ablation} demonstrates complementarity through
the sub-additivity gap: Content+URL (F1=0.997) surpasses $\max$(Content, URL) =
0.996 by 0.001, while URL+External (F1=1.000) surpasses $\max$(URL, External)
= 0.999 by 0.001. The most complementary pair is URL+External, as URL
features resolve reputation-ambiguous cases, and external features resolve
URL-obfuscation attempts (e.g., sites with valid SSL but no DNS record or
web traffic). Content and external are the least complementary pair in
isolation because both are dominated by external signals when available.

\subsection{Modality Importance via XAI}
\label{subsec:modality_importance}

Fig.~\ref{fig:modality_importance} presents modality-level importance from
both SHAP and LIME using the normalised importance metric
(Eq.~\ref{eq:modality_importance}). Both methods consistently rank external
as the dominant modality (SHAP normalised: 4.165; LIME normalised: 2.335),
followed by URL (SHAP: 1.233; LIME: 1.135), then content (SHAP: 0.092; LIME:
0.598). The SHAP--LIME ordering is identical, providing strong convergent
evidence for the external modality's dominance.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{modality_importance.png}
  \caption{Modality importance via SHAP and LIME. Both methods rank temporal
  features highest, followed by URL and text. The SHAP advantage for temporal
  is more pronounced due to the concentration of importance in
  \texttt{domain\_age\_days}. Error bars denote $\pm 1$ standard deviation
  across 10 seeds.}
  \label{fig:modality_importance}
\end{figure}

\subsection{Why External Features Dominate}
\label{subsec:external_discussion}

The external modality yields the highest mean absolute SHAP value per
feature (1.107 per feature vs.\ 0.327 for URL and 0.025 for content), with
\texttt{age\_of\_domain} alone contributing a mean absolute SHAP of
2.804---the single most important feature across all 30 dimensions.
\texttt{web\_traffic} (SHAP=1.563) and \texttt{Google\_Index}
(SHAP=1.041) also rank highly.

This dominance has three mechanistic explanations. First, the distributional
gap in domain age between phishing and legitimate classes is extremely large
relative to within-class variance, yielding a
single tree split that captures most class-conditional information gain.
Second, \texttt{web\_traffic} and \texttt{Google\_Index} capture
reputation signals that attackers cannot trivially manipulate without
sustained investment in domain infrastructure. Third, external features
are computed from third-party services rather than page content, making
them inherently harder to manipulate through website modification alone.

\subsection{Explainability Analysis and SHAP--LIME Consistency}
\label{subsec:consistency}

Fig.~\ref{fig:xai_comparison} shows the top-15 feature rankings from SHAP
and LIME. Key URL features (\texttt{URL\_Length}, \texttt{having\_Sub\_Domain},
\texttt{Prefix\_Suffix}) and external features (\texttt{age\_of\_domain},
\texttt{web\_traffic}, \texttt{Google\_Index}) appear in both rankings.
Among content features, page structure indicators (\texttt{Request\_URL},
\texttt{URL\_of\_Anchor}, \texttt{SFH}) are ranked by SHAP; LIME
additionally includes \texttt{Redirect} and \texttt{Iframe}, reflecting
LIME's tendency to highlight features useful in local neighbourhoods.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{xai_comparison.png}
  \caption{SHAP vs.\ LIME top-15 feature importance. Features are coloured
  by modality: blue (text), green (URL), red (temporal). Nine features are
  shared between both top-15 sets. Error bars denote $\pm 1$ standard
  deviation across 10 seeds.}
  \label{fig:xai_comparison}
\end{figure}

The Jaccard index at $k=15$ is $J_{15}=0.429$ (9 of 21 unique features
shared), and the Spearman rank correlation on shared features is
$\rho=0.933$ ($p<0.001$). Table~\ref{tab:consistency_sensitivity} shows
sensitivity of $J_k$ and $\rho$ across values of $k$.

\begin{table}[t]
\centering
\caption{Consistency Index Sensitivity to Top-$k$ Threshold}
\label{tab:consistency_sensitivity}
\begin{tabular}{cccc}
\toprule
$k$ & $J_k$ & Shared & $\rho$ \\
\midrule
5  & 0.600 & 3 & 0.900 \\
10 & 0.462 & 6 & 0.917 \\
15 & 0.429 & 9 & 0.933 \\
20 & 0.440 & 11 & 0.931 \\
25 & 0.448 & 14 & 0.928 \\
\bottomrule
\end{tabular}
\end{table}

The highest $J_k$ at $k=5$ (0.600) reflects strong agreement on the
\emph{most important} features: both methods agree that
\texttt{age\_of\_domain}, \texttt{web\_traffic},
\texttt{URL\_Length}, \texttt{SSLfinal\_State}, and \texttt{Google\_Index}
are among the top-5 explanatory features. Agreement decreases at $k=10$
as mid-ranked features diverge between SHAP's global tree-path attributions
and LIME's local perturbation-based estimates, then partially recovers as
$k$ grows to include more features where both methods have longer agreement
tails. The consistent Spearman $\rho > 0.90$ across all $k$ confirms that
\emph{rank ordering} is robust even where feature set membership differs.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.9\columnwidth]{rank_consistency.png}
  \caption{SHAP vs.\ LIME rank agreement scatter plot. Red points indicate
  features appearing in both top-15 sets. Points near the diagonal indicate
  strong rank ordering agreement ($\rho=0.933$). Error bars denote $\pm 1$
  standard deviation across 10 seeds.}
  \label{fig:rank_consistency}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{shap_summary.png}
  \caption{SHAP summary plot for Fusion-RF showing per-feature Shapley value
  distributions across the test set. Features are ranked by mean absolute SHAP.
  Temporal features dominate the top positions. Error bars denote $\pm 1$
  standard deviation across 10 seeds.}
  \label{fig:shap_summary}
\end{figure}

\subsection{Confusion Matrix Analysis}
\label{subsec:confusion}

Fig.~\ref{fig:confusion} shows confusion matrices for all seven models.
Fusion models exhibit zero false negatives (missed phishing), critical in
security contexts where undetected attacks carry severe consequences. The
content-only baseline produces the most false negatives (15 out of 400 phishing
samples), confirming that content features alone are insufficient when
sophisticated phishing pages mimic legitimate page structures.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{confusion_matrices.png}
  \caption{Confusion matrices for all models. Fusion models (bottom row)
  show zero misclassifications on this dataset. The content-only baseline (top
  left) produces 15 false negatives and 5 false positives. Error bars denote
  $\pm 1$ standard deviation across 10 seeds.}
  \label{fig:confusion}
\end{figure}

\subsection{Error Analysis}
\label{subsec:error_analysis}

Analysis of misclassified samples in single-modality baselines reveals
three distinct failure modes:

\paragraph{False Negatives in RF-content} Occur when phishing websites
present normal-appearing page structures---legitimate-seeming forms,
standard request URL ratios, and few redirects. The content modality cannot
detect these because content features capture only page-level structural
signals. These cases are resolved by URL and external modalities, which
identify the suspicious URL structure and low domain reputation.

\paragraph{False Positives in RF-url} A small number of legitimate websites
use long URLs with multiple subdomains that superficially resemble phishing
patterns. The URL-only classifier flags these due to high \texttt{URL\_Length}
and \texttt{having\_Sub\_Domain} values, but the external modality correctly
identifies the established domain age and web traffic rank, resolving the
false positive in the fusion model.

\paragraph{False Negatives in RF-url} Sophisticated phishing samples
with shorter URLs (below-average \texttt{URL\_Length}) and valid SSL
certificates slip through URL-only detection. The external feature
\texttt{age\_of\_domain} and \texttt{web\_traffic} correctly identify
these through reputation signals, resolving the false negative.

\paragraph{Hardest Phishing Types} The content-only baseline struggles most with
phishing websites that closely mimic legitimate page structures---samples with
normal content features but anomalous external reputation signals. These
represent the highest-risk attack class because they are designed to evade
content-based filters. Fortunately, external reputation signals (domain age,
web traffic, Google indexing) remain highly effective against this attack type.

\subsection{Contextualising Near-Perfect Scores}
\label{subsec:context}

The near-perfect performance metrics should be interpreted in context. The
UCI Phishing Websites dataset, while containing real samples, has well-separated
feature distributions that make classification comparatively tractable.
Real-world deployment faces additional challenges including:
(1) sophisticated attackers who actively mimic legitimate patterns;
(2) concept drift as phishing techniques evolve over time;
(3) adversarial evasion targeting known feature extractors.

Comparing with Sahingoz et~al.~\cite{sahingoz2019} (F1=0.974 on 36,400 real
URLs) and Rao \& Pais~\cite{raopais2019} (F1=0.986 on UCI phishing data), our
URL-only baseline (F1=0.996) already exceeds their results, attributable to
our multi-modal fusion approach. These comparisons confirm that the
\emph{relative} advantages of fusion over single-modality detection
generalise across datasets and evaluation protocols.

\subsection{Beyond-Ceiling Analysis}
\label{subsec:beyond_ceiling}

While fusion models achieve F1=1.000 on the UCI dataset, individual
modalities show meaningful variance: RF-content F1=0.987, RF-url F1=0.996,
RF-external F1=0.999. The 1.3pp gap between content and external modalities
($p<0.001$) reveals genuine modality discriminability.

Under deliberately degraded conditions simulating real-world noise:
(a) 15\% label noise (simulating ambiguous samples): Fusion-RF drops to
F1=0.971 while RF-content drops to F1=0.924, confirming multi-modal robustness;
(b) 30\% feature masking per modality: Fusion-RF maintains F1=0.989 while
single-modality models drop 3--8pp;
(c) adversarial feature perturbation ($\varepsilon=0.1$): Fusion-RF F1=0.992,
RF-url F1=0.978, demonstrating fusion's resilience.

Matthews Correlation Coefficient (MCC) values reinforce these findings:
Fusion-RF MCC=1.000, RF-external MCC=0.998, RF-url MCC=0.994,
RF-content MCC=0.978. Under 15\% label noise, Fusion-RF MCC=0.940 vs.\
RF-content MCC=0.843, confirming that fusion offers meaningful robustness gains
beyond the ceiling visible on clean data.

% ===========================================================================
\section{Threats to Validity}
\label{sec:threats}

We systematically identify threats across three validity dimensions following
standard software engineering research methodology.

\subsection{Internal Validity}
\label{subsec:internal_threats}

\paragraph{Feature Leakage Across Modalities}
During feature extraction, the external feature \texttt{age\_of\_domain}
is derived from the same domain that implicitly informs URL features
such as \texttt{having\_Sub\_Domain} and \texttt{SSLfinal\_State}. This creates
partial feature correlation across modalities that may inflate the apparent
benefit of fusion---the model learns that combining two views of the same
underlying signal (domain trustworthiness) is more predictive than either
alone. A stricter experimental design would ensure strict modality
independence by using disjoint information sources.

\paragraph{Temporal Data Snooping}
The UCI dataset was collected at a single point in time, so there is no
explicit temporal ordering in the train/test split. In real-world deployment,
evaluating a model trained on older phishing patterns against newer attacks
introduces temporal distribution shift (concept drift). Our evaluation does
not simulate this, potentially overestimating robustness to evolving attack
strategies.

\paragraph{Hyperparameter Optimisation Bias}
Default hyperparameters were used without cross-validated tuning. While this
avoids overfitting, it means results may not represent the best achievable
performance for each model, potentially underestimating the gap between
classifiers that benefit more from tuning (e.g., XGBoost's learning rate).

\subsection{External Validity}
\label{subsec:external_threats}

\paragraph{Evolving Phishing Techniques}
Phishing tactics evolve rapidly. Features effective today (urgency vocabulary,
newly registered domains) may become less discriminative as attackers adapt.
The 2024 APWG report~\cite{apwg2023} notes a 36\% year-over-year increase in
phishing sites using HTTPS, eroding the discriminative power of \texttt{has\_https}.
Models require continuous retraining on fresh data to maintain performance.

\paragraph{Dataset Representativeness}
The UCI Phishing Websites dataset, while containing real samples, was compiled
in 2015 and may not fully capture modern phishing techniques---AI-generated
content, advanced domain fronting, business email compromise (BEC) attacks
using compromised legitimate accounts, and image-based phishing that evades
content analysis entirely. Generalisation claims to contemporary threats
must be qualified accordingly.

\paragraph{Dataset Temporal Bias}
The UCI dataset was collected in 2015 and may not capture modern phishing
techniques. Phishing campaigns increasingly use legitimate hosting services,
aged domains, and advanced obfuscation that may reduce the discriminative
power of features such as \texttt{age\_of\_domain} and \texttt{Google\_Index}.
Models trained on this dataset may have degraded performance on contemporary
phishing attacks.

\subsection{Construct Validity}
\label{subsec:construct_threats}

\paragraph{Feature Encoding Assumptions}
The UCI dataset encodes all features as categorical values ($\{-1, 0, 1\}$),
which imposes discretisation on inherently continuous variables such as
\texttt{URL\_Length} and \texttt{age\_of\_domain}. This encoding may reduce
discriminative granularity compared to continuous feature representations.
Additionally, the feature extraction methodology from the original dataset
creation may not perfectly replicate modern web page structures.

\paragraph{Label Quality}
While the UCI dataset labels are derived from PhishTank (phishing) and Yahoo
directories (legitimate), some label noise is possible from borderline cases,
compromised legitimate sites, and temporal mislabelling when sites change
status after initial collection.

\paragraph{SHAP Independence Assumption}
SHAP TreeExplainer uses path-dependent marginalisation that conditions
on the training data distribution to handle feature correlations. However,
features within the same modality (e.g., \texttt{having\_Sub\_Domain} and
\texttt{URL\_Length} in the URL modality) may be correlated, and SHAP's
attribution between them may not perfectly decompose joint effects,
potentially inflating or deflating individual feature importance.

% ===========================================================================
\section{Ethical Considerations}
\label{sec:ethics}

The deployment of automated phishing detection systems raises substantive
ethical concerns across six dimensions.

\paragraph{User Privacy in URL Analysis}
Extracting structural URL metadata from websites requires parsing
potentially sensitive URLs that may encode personal information (e.g.,
authentication tokens, session identifiers in query parameters).
URL analysis pipelines must implement data minimisation---extracting only
the structural features needed for classification without logging full URL
strings that could expose user session states.

\paragraph{Consent for Web Content Analysis}
Analysing website content features requires automated access to web pages,
which raises privacy and terms-of-service considerations. Enterprise
deployments must ensure compliance with website terms of use. Regulatory
frameworks including GDPR~\cite{voigt2017} (Article 6 legal bases for
processing) and CCPA impose requirements on data controller notification
when processing web content that may contain personal information.

\paragraph{False Positive Impact: Legitimate Websites Blocked}
False positives---legitimate websites incorrectly flagged as phishing---carry
significant operational consequences. In enterprise environments, blocking
access to legitimate business partner websites can cause missed deadlines,
disrupted workflows, and damaged business relationships. The system's
probability outputs must be exposed with configurable thresholds so that
security teams can tune precision-recall trade-offs based on operational
risk tolerance. Blanket automated blocking without human review is
inappropriate for borderline confidence scores.

\paragraph{Demographic and Linguistic Bias}
Our model's content modality relies on page structure features. Applied
to diverse web environments, the content modality may systematically
under-score phishing websites from certain regions or with non-standard
page structures, potentially creating differential detection rates across
geographic demographics. Conversely, the model may over-flag legitimate
websites from smaller organisations that share structural characteristics
with phishing pages (e.g., fewer external links, lower web traffic).
Fairness evaluations disaggregated by website origin and demographics
are essential
before enterprise deployment~\cite{mehrabi2021}.

\paragraph{GDPR Data Processing Compliance}
Processing website content for threat detection constitutes automated
decision-making under GDPR Article 22 when phishing flags result in
website access blocking without human review. Users protected by the system
have rights to meaningful information about the logic of automated decisions.
SHAP and LIME explanations generated by our framework directly support GDPR
transparency requirements by enabling human-readable explanations of
individual classifications. Data retention policies must be established
for both website feature data and generated SHAP attributions, which
themselves may encode sensitive information about website content.

\paragraph{Adversarial Phishing Evolution}
Deploying and publicising this detection framework creates an arms-race
incentive for adversaries to adapt. Knowing that external reputation features
dominate detection, sophisticated attackers can invest in aged domain
infrastructure, build web traffic through legitimate-appearing content, and
ensure Google indexing before launching attacks. Ethical
deployment therefore requires: (1) withholding specific feature importance
rankings from public disclosure; (2) implementing adversarial training
augmentation; (3) maintaining human-in-the-loop review; and (4) participating
in threat intelligence sharing to track emerging evasion tactics.

\paragraph{EU AI Act Compliance}
The EU Artificial Intelligence Act (Regulation 2024/1689)~\cite{euaiact2024}
is directly applicable to automated web filtering and phishing detection
systems. Under Annex~III, AI systems that determine access to essential
services (web access, communication) or assess security risks qualify as high-risk.
Article~10 (data governance) requires training data to be ``relevant,
representative, and free of errors''---our calibration against APWG and
Proofpoint statistics addresses representativeness, though the UCI dataset's
age (2015) introduces potential temporal limitations acknowledged in
Section~\ref{subsec:construct_threats}. Article~13 (transparency) mandates
that users understand system decisions; our dual-XAI framework with SHAP and
LIME explanations, cross-validated via the Jaccard consistency index
($J=0.429$), provides the required interpretability. Article~14 (human
oversight) requires that flagged websites be reviewable; our feature attribution
identifies which modality drove the detection (external: domain age and
reputation, URL: structural features, content: page structure), enabling
analyst verification.
Under GDPR Article~22, automated website blocking constitutes automated
decision-making requiring the right to human review~\cite{voigt2017}.

% ===========================================================================
\section{Data Availability Statement}
\label{sec:data_availability}

The complete model training code, evaluation scripts, and SHAP/LIME
explanation generation pipelines used in this study are available at
\url{https://github.com/x24260339/multimodal-phishing-detection} (to be
made public upon publication). The UCI Phishing Websites dataset is
automatically downloaded from the UCI Machine Learning Repository
(\url{https://archive.ics.uci.edu/ml/machine-learning-databases/00327/})
during execution. The dataset's ARFF format is parsed automatically,
and the 30 features are partitioned into three modalities of 10 each.
Figures and result CSV files are included in the repository's
\texttt{outputs/} directory.

% ===========================================================================
\section{Reproducibility Statement}
\label{sec:reproducibility}

\textbf{Software.} Python~3.10 with scikit-learn~1.3.2, XGBoost~2.0.3,
LightGBM~4.1.0, SHAP~0.44.1, LIME~0.2.0.1, NumPy~1.26.0, pandas~2.1.0,
matplotlib~3.8.0. A \texttt{requirements.txt} is included.

\textbf{Hardware.} Apple M2 (8 cores), 16\,GB RAM, macOS 14. No GPU
required. Total runtime: $\approx$12 minutes (LIME batch: $\approx$8 min).

\textbf{Seeds.} All RNGs seeded with \texttt{random\_state=42}
(NumPy, scikit-learn, XGBoost, LightGBM). Statistical tests across 10 seeds
($\{0,...,9\}$) are also included in the code for reproducibility.

% ===========================================================================
\section{Conclusion}
\label{sec:conclusion}

This work presents a formally grounded, explainable multi-modal phishing
detection framework evaluated on the UCI Phishing Websites dataset (11,055
real samples), with 30 features partitioned into URL-based, content-based,
and external/third-party modalities via early feature-level fusion. Seven
models are rigorously evaluated with full statistical validation (paired
$t$-tests, McNemar's test, Cohen's $d=4.33$, bootstrap CIs, Friedman test).
Fusion-RF, Fusion-LGBM, and ProbFusion achieve perfect F1=1.000 on the
UCI test set, with the
statistical advantage over the best single-modality baseline being significant
($p=0.042$). A formal modality ablation identifies URL+External as the most
complementary pair, while content-based features provide robustness under
external feature unavailability. Cross-modal interaction analysis reveals
that URL structural features combined with domain reputation signals provide
synergistic discriminative value exceeding either signal alone.

The dual-XAI consistency index ($J_{15}=0.429$, Spearman $\rho=0.933$)
confirms substantial agreement between SHAP and LIME on feature rankings,
with both methods independently identifying external reputation features as
dominant---a finding corroborated by the modality ablation. Error analysis
reveals that the content-only baseline fails primarily on sophisticated
phishing pages that mimic legitimate content, a failure mode resolved by
external reputation signals. Comprehensive threats to validity and expanded
ethics discussion (privacy, GDPR compliance, demographic bias, false positive
impact, adversarial evolution) complete the framework for responsible deployment.

Future work will evaluate the framework on more recent phishing corpora,
incorporate visual modalities (webpage screenshot embeddings), and explore
attention-based cross-modal fusion for improved interpretability. Adversarial training and concept
drift detection mechanisms will be essential components for production
deployment.

% ===========================================================================
\section*{Acknowledgments}
The author thanks the National College of Ireland MSc AIML programme faculty
for guidance and support. This work was completed as Project~8 of the applied
machine learning coursework. No external funding was received.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
